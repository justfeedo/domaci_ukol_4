# Environment configuration
# Set to true to use mock data (client-side simulation)
# Set to false to use real server API
REACT_APP_USE_MOCK_DATA=true

# API server URL (used when mock mode is disabled)
REACT_APP_API_URL=http://localhost:3001
